{% block body %}
    <nav class="navbar navbar-dark bg-dark navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path("app_homepage") }}"><span id="test">Phoenix </span></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                    aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="{{ path("app_homepage") }}"><i
                                class="fa-solid fa-house"></i>Tableau de bord</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_projects") }}"> Mes Projets</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_projects") }}">Portfolio</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_projects") }}">Mon équipe</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("admin") }}">Admin</a>
                </li>
            </ul>
            <div class="d-lg-flex">
                {% if app.user %}
                    <ul class="navbar-nav ml-auto">
                        <li class="nav-item">
                                            <span class="text-light">
                                                <a class="nav-link d-inline-block" href="#"><i class="fa fa-user"></i>
                                            <span class="user-name">{{ app.user.username }}</span></a>
                                                <a class="nav-link d-inline-block" href="{{ path('app_logout') }}">Se Déconnecter</a>
                                            </span>
                        </li>
                    </ul>
                {% else %}
                    <ul class="navbar-nav ml-auto">
                        <li class="nav-item">
                                            <span class="text-light">
                                                <a class="nav-link d-inline-block" href="{{ path('app_login') }}">Se connecter</a>
                                                ou
                                                <a class="nav-link d-inline-block" href="{{ path('app_register') }}">S'enregistrer</a>
                                            </span>
                        </li>
                    </ul>
                {% endif %}
            </div>
        </div>
    </nav>
{% endblock %}
