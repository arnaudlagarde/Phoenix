{{ form_start(form) }}
<div class="d-md-flex">
    <div class="col-12 col-md-6">
        {{ form_label(form.name, "Name") }}
        {{ form_widget(form.name) }}

        <div class="col-12 col-md-12">
            {{ form_label(form.DateFact, "Début") }}
            {{ form_widget(form.DateFact.date) }}
            <div class="d-none">
                {{ form_widget(form.DateFact.time.hour) }}
                {{ form_widget(form.DateFact.time.minute) }}
            </div>
        </div>
        <div class="col-12 col-md-12">
            {{ form_label(form.Critical, "Critical") }}
            {{ form_widget(form.Critical) }}
        </div>
        <div class="col-12 col-md-12">
            {{ form_label(form.Probability, "Probability") }}
            {{ form_widget(form.Probability) }}

            {{ form_label(form.projet, "Projet") }}
            {{ form_widget(form.projet) }}
        </div>
    </div>
</div>

<div class="col-12 col-md-12">
    <button class="btn btn-dark">
        {{ button_label|default('Enregistrer') }}
    </button>
</div>

{{ form_end(form) }}
