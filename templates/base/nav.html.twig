{% block body %}
    <nav class="navbar navbar-dark bg-dark navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path("app_homepage") }}"><span id="test">Phoenix </span></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                    aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{{ path("app_homepage") }}"><i class="fa fa-eye" aria-hidden="true"></i>Tableau de bord</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path("app_projects") }}"><i class="fa fa-eye" aria-hidden="true"></i> Projets</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path("app_register") }}">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path("app_register") }}">Register</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path("admin") }}">Admin</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}
